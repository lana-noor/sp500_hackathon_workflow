# Core agent framework
agent-framework
agent-framework-foundry
agent-framework-anthropic

# Azure AI Projects SDK (for AIProjectClient + agent_reference support)
azure-ai-projects>=2.0.0

# Azure Identity
azure-identity>=1.15.0

# Word document generation
python-docx

# PowerPoint presentation generation (used by portfolio_risk_workflow.py)
python-pptx

# Environment variable loading
python-dotenv

# OpenAI SDK
openai>=1.30.0

# FastAPI backend + SSE (for the React UI)
fastapi>=0.115

# FastMCP — for the custom MCP Servers (Budget, MS News, MS Learn)
fastmcp>=0.4.0

# Uvicorn for serving (included with FastMCP but explicit for clarity)
uvicorn[standard]>=0.30

# Pydantic (dependency of FastMCP)
pydantic>=2.0

# ── Microsoft Weekly Intelligence Brief additional dependencies ────────────────

# RSS feed parsing — used by ms-news-mcp-server.py and ms-learn-mcp-server.py
feedparser>=6.0.0

# HTTP client — used by MCP servers for fallback RSS fetching and MS Learn API
httpx>=0.27.0

# Anthropic SDK — used by HTMLRenderingAgent via MAF Anthropic provider
# Note: the agent_framework.anthropic module wraps this SDK.
anthropic>=0.30.0

# Requirements for ms-news-mcp-server.py
# Minimal install — only what the MCP server needs

# FastMCP framework
fastmcp>=0.4.0

# HTTP client for RSS fallback fetch
httpx>=0.27.0

# RSS feed parser (primary fetch method)
feedparser>=6.0.0
